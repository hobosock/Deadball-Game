use deadball::characters::players::*;
use deadball::characters::teams::*;
use deadball::core::*;

use std::fs;

fn main() {
    // quick test, just print out each step of a game and see if it makes sense
    // 1. generate 2 new teams
    //gen_team(Era::Modern, starters, bench, pitchers, bullpen, name, firstnames, lastnames, logos, mascots, mottos, personalities, backgrounds, locations, name1, name2);
}
